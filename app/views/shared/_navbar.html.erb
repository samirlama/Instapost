<!DOCTYPE html>
<html>
<head>
  <title></title>
  <style type="text/css">
     .search-form 
     {
      position: relative;
      top:8px;
      height:31px;
      padding-left: 10px;
      border-color: #337ab9;
    }

     .search-button
     {
      position: relative;
      top:6px;
      right:3px;
      height:31px;
      border-radius: 0;


     }
  </style>
</head>
<body>
<nav class="navbar navbar-default">
  <div class="container-fluid">
      <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span> 
      </button>
      <% if user_signed_in? %>
      <a class="navbar-brand" href="/posts">Instapost</a>
      <% else %>
      <a class="navbar-brand" href="#">Instapost</a>
      <% end %>

    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        
  <% if user_signed_in? %>
    <li><a href="/users/<%= current_user.id %>" style="display:inline;"><%= image_tag current_user.photo, :style=>"width:45px; height:45px; border-radius:50%;"  %></a></li>
    <li><%= link_to 'Edit Settings', edit_user_registration_path %></li>
    <li><%= link_to 'Log Out', destroy_user_session_path, method: :delete %></li>
    <li><%= link_to 'New Post', new_post_path %></li>
    <li><%= form_tag(searches_path, method: :get) do %>
      <%= text_field_tag :search , params[:search], class: ' search-form' , placeholder: 'Find people' %>
      <%= submit_tag 'Search' ,  :name=> nil , class: 'btn btn-primary search-button'  %>
      <% end %>
     </li> 
  <% else %>
    <li><%= link_to 'Sign Up', new_user_registration_path %></li>
    <li><%= link_to 'Log In', new_user_session_path %></li>
  <% end %>


      </ul>
    </div>
  </div>
  </div>
</nav>
</body>
</html>
